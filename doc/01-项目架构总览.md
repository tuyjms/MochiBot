# 项目架构总览

## 项目简介

**AI女友 (catgirlwindow)** 是一个基于 .NET 10.0 WinForms 的桌面应用，通过 LLM（大语言模型）驱动，实现一个具有情感交互能力的虚拟伴侣。

## 技术栈

- **.NET**: 10.0-windows
- **UI 框架**: Windows Forms
- **LLM SDK**: OpenAI SDK v2.10.0
- **目标框架**: net9.0-windows

## 项目结构

```txt
catgirlwindow/
├── Program.cs                    # 入口点
├── catgirlwindow.csproj          # 项目文件
│
├── Src/                          # 源代码目录
│   ├── Core/                     # 核心层
│   │   ├── Config/               # 配置管理器
│   │   │   ├── ConfigReader.cs
│   │   │   ├── IConfigReader.cs
│   │   │   ├── ILogger.cs
│   │   │   └── Models/           # 配置模型
│   │   │       ├── AppSettings.cs
│   │   │       ├── ModuleSettings.cs
│   │   │       ├── PersonalityConfig.cs
│   │   │       └── ProviderConfig.cs
│   │   ├── Database/             # 数据库业务层
│   │   │   ├── DatabaseService.cs
│   │   │   └── IDatabaseService.cs
│   │   ├── Events/               # 事件调度器 + 事件定义
│   │   │   ├── EventDispatcher.cs    # 事件调度器
│   │   │   ├── IEventDispatcher.cs   # 事件调度器接口
│   │   │   ├── EventTypes.cs         # 事件枚举和定义
│   │   │   └── EventHandlerBase.cs   # 事件处理器基类
│   │   └── Models/               # 核心数据模型
│   │       ├── AgentMood.cs      # 心情枚举
│   │       ├── ChatMessage.cs
│   │       ├── UserConfig.cs
│   │       └── WeatherInfo.cs
│   │
│   ├── Agent/                    # Agent层
│   │   ├── Agent.cs              # Agent核心协调层
│   │   ├── IAgent.cs             # Agent接口
│   │   ├── AgentMoodTracker.cs   # 心情记录器（集成到Agent）
│   │   ├── IAgentMoodTracker.cs
│   │   ├── ShortTermMemory.cs    # 短期记忆（集成到Agent）
│   │   ├── IShortTermMemory.cs
│   │   ├── LongMemory.cs         # 长期记忆（中期+长期合并）
│   │   ├── ILongMemory.cs
│   │   ├── PromptFormatter.cs    # Prompt格式化器
│   │   └── IPromptFormatter.cs
│   │
│   ├── Services/                 # 服务层
│   │   ├── ToolManager.cs        # 工具管理器（统一调度）
│   │   ├── IToolManager.cs
│   │   ├── LlmClient.cs          # LLM客户端
│   │   ├── DllModModule/         # DLLMOD插件模块
│   │   │   ├── IDllMod.cs
│   │   │   └── IDllModLoader.cs
│   │   ├── McpModule/            # MCP服务管理
│   │   │   └── (MCP相关文件)
│   │   └── Config/               # 服务配置模型
│   │       └── Models/
│   │
│   ├── UI/                       # UI层
│   │   ├── Form1.cs
│   │   └── Form1.Designer.cs
│   │
│   ├── Renderer/                 # 渲染层
│   │   └── ICharacterRenderer.cs # 渲染接口
│   │
│   └── Router/                   # 路由层（feature：多平台支持）
│       └── .gitkeep
│
├── Resources/                    # 资源文件
│   ├── Audio/                    # 音频资源
│   ├── Data/                     # 数据文件
│   ├── Images/                   # 情绪表情图片
│   ├── Person/                   # 人格配置文件
│   └── Mods/                     # DLLMOD插件存放目录
│
├── doc/                          # 项目文档
│   ├── 00 - 写给viber的话.md
│   ├── 01-项目架构总览.md
│   ├── 02-事件系统.md
│   ├── 03-Agent核心协调层.md
│   ├── 04-长期记忆模块.md
│   ├── 05-数据库业务层.md
│   ├── 06-工具管理器.md
│   ├── 07-Prompt格式化器.md
│   ├── 08-配置读取器.md
│   ├── 09-角色动画渲染器.md
│   └── 11-接口调用关系与协作图.md
│
└── catgirlwindow.Tests/          # 测试项目
    ├── catgirlwindow.Tests.csproj
    ├── Services/
    │   ├── AgentMoodTrackerTests.cs
    │   ├── AutoEventServiceTests.cs
    │   ├── ConfigReaderTests.cs
    │   ├── DatabaseServiceTests.cs
    │   ├── PromptFormatterTests.cs
    │   ├── ShortTermMemoryTests.cs
    │   └── ToolServiceTests.cs
    └── Models/
```

## 模块职责概览

### 核心层 (Src/Core)

- **配置管理器** (`Src/Core/Config/`): 统一管理 appsettings.json 和人格配置文件的读取，提供强类型配置访问
- **数据库业务层** (`Src/Core/Database/`): 数据持久化（配置、聊天记录、情绪日志）
- **事件调度器** (`Src/Core/Events/`): 统一的事件分发中心，所有事件（用户输入、系统自动事件、工具结果等）都通过事件调度器分发
- **事件定义** (`Src/Core/Events/EventTypes.cs`): 事件基础类型枚举、事件模型定义
- **心情枚举** (`Src/Core/Models/AgentMood.cs`): AI女友情绪状态枚举

### Agent层 (Src/Agent)

- **Agent核心协调层** (`Src/Agent/Agent.cs`): 系统的"大脑"，协调所有子模块，处理事件调度器分发的事件
- **心情记录器** (`Src/Agent/AgentMoodTracker.cs`): 集成到Agent中，管理AI女友情绪状态
- **短期记忆** (`Src/Agent/ShortTermMemory.cs`): 集成到Agent中，存储最近N条对话记录
- **长期记忆** (`Src/Agent/LongMemory.cs`): 中期记忆和长期记忆合并，统一管理重要信息的存储和检索
- **Prompt格式化器** (`Src/Agent/PromptFormatter.cs`): 通用模板格式化工具

### 服务层 (Src/Services)

- **工具管理器** (`Src/Services/ToolManager.cs`): 统一调度所有工具（基础工具、心情特色工具、MCP工具、DLLMOD插件）
- **LLM客户端** (`Src/Services/LlmClient.cs`): LLM API调用封装
- **DLLMOD插件模块** (`Src/Services/DllModModule/`): 动态加载DLL作为插件
- **MCP服务管理** (`Src/Services/McpModule/`): 管理MCP服务器工具

### UI层 (Src/UI)

- **主窗口** (`Src/UI/Form1.cs`): 只负责显示和用户输入，所有逻辑通过事件调度器走 Agent 统一调度

### 渲染层 (Src/Renderer)

- **角色动画渲染器接口** (`Src/Renderer/ICharacterRenderer.cs`): 角色动画渲染接口

### 路由层 (Src/Router)

- 预留的多平台支持模块（QQ平台等）

## 事件流向（核心架构变更）

```
用户交互 / 系统自动事件
        │
        ▼
  事件调度器 (EventDispatcher)
        │
        ├── 事件分类打包（EventCategory + EventType + Info）
        │
        ▼
  Agent 核心协调层
        │
        ├── 消息预处理 / 情绪判断 / 短期记忆记录
        │
        ▼
  LLM 处理（对话模式 / 函数模式）
        │
        ▼
  Agent 解析 LLM 响应（actions）
        │
        ▼
  工具管理器 (ToolManager)
        │
        ├── 基础工具（timer / compliment / pet / weather）
        ├── 心情特色工具（hug / dance / tuck_in / cuddle / calm_down）
        ├── MCP工具（通过MCP服务管理）
        └── DLLMOD插件（通过DLLMOD加载器）
              │
              ▼
        结果返回 Agent → 回复用户
```

## 分层架构

项目分为五层，从上到下依次为：

1. **UI 层 (Form1)**: 只负责显示和用户输入，所有逻辑通过事件调度器走 Agent 统一调度
2. **事件调度层 (EventDispatcher)**: 统一的事件分发中心，解耦事件源和事件处理器
3. **Agent 核心协调层**: 作为 LLM 与平台交互的唯一入口，内部协调所有子模块
4. **服务层 (Services)**: 工具管理器、LLM客户端、DLLMOD插件、MCP服务
5. **基础设施层**: 配置管理器、数据库业务层、渲染器、路由层

## 核心架构原则

1. **事件驱动架构**: 所有交互（用户输入、系统事件、工具结果）都通过事件调度器分发
2. **Agent 是唯一入口**: Agent 处理所有事件，协调各模块
3. **工具管理器统一调度**: 基础工具、心情工具、MCP工具、DLLMOD插件统一由 ToolManager 管理
4. **两种 LLM 模式**: 对话模式（LLM扮演女友人格）和函数模式（LLM作为纯函数）
5. **面向接口编程**: 所有模块通过接口交互，便于单元测试和模块替换
6. **记忆模块集成化**: 短期记忆集成到Agent中，中期和长期记忆合并为 LongMemory
