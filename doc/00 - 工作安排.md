# 工作安排

## 协作须知

请先阅读 `doc/00 - 写给viber的话.md`，了解本项目的基本协作规范：

- **Git 提交格式**：`影响程度词：一句话描述`（如 `功能改动：添加天气查询模块`）
- **配置管理规范**：所有配置项必须通过 **ConfigReader** 统一管理，禁止硬编码
- **日志规范**：所有模块必须通过 ConfigReader 的 Logger 打印日志，禁止 `Console.WriteLine`
- **配置项流程**：发起协商 → 注册到配置读取器文档 → 实现代码 → 更新文档

---

## 近期已完成

| 提交 | 说明 |
|------|------|
| `966dd42` | VRM 鼠标穿透透明度修复 + 术语文档 + CodeGraph 优先规则 |
| `13178df` | VRM 事件驱动表情系统 + MainWindow WebView2 重设计 + 鼠标穿透 + 关闭行为配置 |
| `b9cba94` | SettingsWindow 人格增删功能，ConfigReader DeletePersonality API |
| `374e4ea` | Agent 状态机 + 事件队列，EventDispatcher 模块状态登记 |
| `4e8a36a` | LlmClient 重试 + 超时配置，单元测试（8 个） |
| `0478c35` | 硬编码提取为常量，SettingsWindow → TabControl |
| `357f1cb` | 修复测试套件 ConfigReader 单例冲突 |
| `db4a2df` | VRM 3D 模型查看器和 VRMA 动作驱动 |

---

## 一、设置窗口 UI（SettingsWindow）— 主体已完成

`0478c35` 改造为 TabControl + 多目标 Save 分发，`b9cba94` 完成人格增删功能。关闭行为配置（Exit/Hide）已完成。

以下子任务待完成：

### 3️⃣ Cron 表达式辅助

- **下拉菜单预设**：
  - 每 N 分钟：`*/5 * * * *`、`*/10 * * * *`、`*/25 * * * *`、`*/30 * * * *`
  - 每小时：`0 * * * *`
  - 每天固定时间：`0 9 * * *`、`0 23 * * *`
  - "自定义"：允许手动输入
- **说明提示**（ToolTip）：5 字段格式说明（分 时 日 月 周）

### 4️⃣ 内置任务配置

对应 `Resources/appsettings.json` 中的 `CronTasks[]` 数组，当前已有 4 个内置任务：

| 任务 | 字段 | 控件类型 |
|------|------|----------|
| 碎碎念（builtin:murmur） | 启用开关 + 权重滑动条(0-100) | CheckBox + Slider |
| 用眼提醒（builtin:eye_rest） | 启用开关 + 阈值分钟数输入 | CheckBox + TextBox |
| 深夜关怀（builtin:late_night） | 启用开关 + 偏移范围滑动条(-30~30) | CheckBox + Slider（双值） |
| 空闲检测（builtin:idle_check） | 启用开关 + 阈值分钟数输入 | CheckBox + TextBox |

- 保存后调用 `EventDispatcher.SetTaskEnabled(taskId, enabled)` 实时生效
- 参数变更需重复写入 `appsettings.json`，调用 `ConfigReader.Reload()` 刷新

---

## 二、事件调度器（EventDispatcher）— 基本完成

对应文档 `doc/02-事件系统.md`，模块位置 `Src/Core/Events/`

- [ ] **异常处理**：`PublishAsync` 中 `Task.WhenAll` 会吞掉订阅者异常，需要更精细的错误隔离

---

## 三、Agent 核心协调层 — 已完成

Agent 不需要单元测试，正确性通过集成测试验证。

---

## 四、工具系统（ToolService）— 基础完成

对应文档 `doc/06-工具管理器.md`，模块位置 `Src/Services/Tools/ToolService.cs`

- [ ] **MCP 工具**：`ActionExecutor.HandleMcpCallAsync` 是空实现（仅 Debug 日志），需对接 MCP 服务
- [ ] **插件管理 UI**：无可视化的 DLLMOD 插件列表/启用/禁用界面
- [ ] **工具热重载**：配置变更后工具列表未动态刷新

---

## 五、数据库 & Repository — 已完成

---

## 六、渲染器（Renderer）— 功能完整

- [ ] **素材扩充**：当前每个情绪只有 1-2 个动作，资源文件位于 `Resources/Images/{情绪名}/{动作名}/`

---

## 七、LLM 客户端（LlmClient）— 基础完成

- [ ] **流式响应**：当前是阻塞式 `CompleteChatAsync`

---

## 八、桌宠显示架构（方案 C）— 已完成

MainWindow 作为轻量托盘，桌宠显示在独立窗口中（GIF/VRM），通过 `IDisplayWindow` 接口统一管理。已删除旧 `VrmViewerWindow`。

### 待完成

- [ ] 窗口位置/大小持久化（GIF 和 VRM 窗口均未实现）

### （可选）后续扩展

- LLM 工具调用表情（`play_expression` 工具）
- 精简模式（关闭显示窗口只留托盘）/ 沉浸模式（隐藏托盘只留显示窗口）
- 外部 `.vrma` 动作文件
- 运行时切换 GIF ↔ VRM

---

## 十、长期记忆模块接入 — 待开发

详细计划见 `doc/05-长期记忆模块开发计划.md`。

**现状**：`LongMemory` 代码完整（接口/实现/Repository/测试/配置/UI），但 Agent 从未调用——是座孤岛。

**4 步接入**：

| 步骤 | 内容 | 改动范围 |
|------|------|----------|
| 1. 上下文注入 | 对话时检索长期记忆注入 LLM 上下文 | `Agent.cs` — `UserContextTemplate` + `BuildUserContextAsync` |
| 2. 记忆录入 | 短期记忆溢出时触发 `SummarizeShortTermAsync` | `Agent.cs` — 对话完成后检查 `IsSummarizePending` |
| 3. 定期维护 | Timer 驱动晋升/淘汰 | `Agent.cs` — 内部定时器 |
| 4. 状态修复 | `GetStatus()` 返回真实 LongTermMemoryCount | `Agent.cs` |

---

## 九、技术债 & 潜在问题

1. **空 catch 模式**：多处 `try { } catch { }`（EventDispatcher.Publish、BuiltinTaskHandler 等），可能掩盖真实问题
2. **线程安全**：`EventDispatcher.CheckCronTasks` 在 Timer 线程同步调用 `Publish`，可能阻塞 Timer；`BuiltinTaskHandler._isHandling` 多事件并发问题
3. **XAML 硬编码**：`ChatWindow.xaml` 中 `Title="小琪"` 和 `Text="小琪"` 是设计时默认值，可改为数据绑定

---

## 优先级建议

### P1

| 任务 | 涉及文档 | 理由 |
|------|----------|------|
| 长期记忆模块接入 | `doc/05-长期记忆模块开发计划.md` | 代码已完成但未接入 Agent，核心功能缺失 |

### P2

| 任务 | 涉及文档 | 理由 |
|------|----------|------|
| MCP 工具实现 | `doc/06-工具管理器.md` | 功能拓展 |
| EventDispatcher 异常隔离 | `doc/02-事件系统.md` | `PublishAsync` 中 `Task.WhenAll` 会吞掉订阅者异常 |

### P3

| 任务 | 涉及文档 | 理由 |
|------|----------|------|
| 插件管理 UI（DLLMOD 可视化列表） | `doc/06-工具管理器.md` | 用户体验 |
| 工具热重载 | `doc/06-工具管理器.md` | 配置变更后工具列表未动态刷新 |
| 流式响应 | — | 体验优化 |
| 技术债清理（空 catch、线程安全、XAML 硬编码） | — | 代码质量 |
