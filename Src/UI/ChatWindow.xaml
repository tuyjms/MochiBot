<Window x:Class="MochiBot.Src.UI.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="小琪" Height="520" Width="380"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">
    <Border CornerRadius="10" Background="White" BorderBrush="#FFE0E0E0" BorderThickness="1"
            MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.2" Color="Black"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="#FFF5F5F5" BorderBrush="#FFE0E0E0" BorderThickness="0,0,0,1">
                <Grid Height="48" Margin="12,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <!-- 头像 -->
                    <Border Grid.Column="0" Width="32" Height="32" CornerRadius="16" Background="#FFE8D5C4"
                            VerticalAlignment="Center">
                        <TextBlock x:Name="characterAvatarText" Text="小" FontSize="14" FontWeight="Bold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Foreground="#FF8B6F5E"/>
                    </Border>
                    <!-- 名称 -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                        <TextBlock x:Name="characterNameText" Text="小琪" FontSize="15" FontWeight="SemiBold"/>
                        <TextBlock Text="在线" FontSize="11" Foreground="#FF4CAF50"/>
                    </StackPanel>
                    <!-- 关闭按钮 -->
                    <Button Grid.Column="2" Content="✕" Width="30" Height="30"
                            FontSize="14" Background="Transparent" BorderThickness="0"
                            Foreground="#FF888888" Click="CloseButton_Click"
                            VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- 消息列表 -->
            <ListBox Grid.Row="1" x:Name="messageList"
                     Background="#FFF8F8F8"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     VirtualizingPanel.ScrollUnit="Pixel"
                     HorizontalContentAlignment="Stretch">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter HorizontalAlignment="Stretch"/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <!-- Grid.Stretch 保证子元素测量时获得有限宽度，TextWrapping 生效 -->
                        <Grid>
                            <Border CornerRadius="10" Margin="8,4"
                                    MaxWidth="280"
                                    Padding="10,8"
                                    BorderThickness="0"
                                    HorizontalAlignment="Right">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#FF4A90D9"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsUser}" Value="False">
                                                <Setter Property="Background" Value="#FFFFFFFF"/>
                                                <Setter Property="BorderBrush" Value="#FFE0E0E0"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Setter Property="HorizontalAlignment" Value="Left"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Text}"
                                           TextWrapping="Wrap"
                                           FontSize="14">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="White"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUser}" Value="False">
                                                    <Setter Property="Foreground" Value="#FF333333"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- 输入区域 -->
            <Border Grid.Row="2" Background="#FFF5F5F5" BorderBrush="#FFE0E0E0" BorderThickness="0,1,0,0">
                <Grid Margin="12,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="inputBox" Grid.Column="0"
                             Height="36" FontSize="14"
                             VerticalContentAlignment="Center"
                             Padding="10,0"
                             BorderBrush="#FFD0D0D0"
                             BorderThickness="1"
                             Background="White"
                             KeyDown="InputBox_KeyDown"/>
                    <Button x:Name="sendButton" Grid.Column="1"
                            Content="发送" Width="60" Height="36"
                            Margin="8,0,0,0"
                            FontSize="14"
                            Background="#FF4A90D9"
                            Foreground="White"
                            BorderThickness="0"
                            Click="SendButton_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
